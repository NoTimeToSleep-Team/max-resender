# Контекст текущего подэтапа

## Активная версия

- Версия: `v0.02.01`
- Статус: `active`
- Дата фиксации: `2026-04-21`

## Цель подэтапа

Зафиксировать стартовый аудит, архитектуру и roadmap реального проекта `project-max`, чтобы перейти от шаблона к первой кодовой итерации.

## Что уже сделано

- прочитан весь доступный контекст рабочей области и проекта;
- обнаружено, что репозиторий пока остаётся шаблонным и не содержит кода, конфигов и инфраструктуры;
- выявлен контекстный drift: часть файлов всё ещё описывает `project1` вместо `project-max`;
- подтверждена необходимость сменяемого адаптерного слоя для MAX и минимальной схемы развёртывания для Raspberry Pi.

## Что не входит в подэтап

- полная реализация backend, frontend и интеграций;
- end-to-end проверка внешних сервисов без реальных токенов и доступов.

## Следующий логичный шаг

- `v0.02.02`: развернуть Node.js/TypeScript workspace, базовый backend с healthcheck, SQLite schema, env validation и каркас React/PWA.
